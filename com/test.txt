/*
  test code version 2.0
*/
struct Name {
    int temp;
    int attribute = 0; 
    char sympol;
};
void main() {
    // [test] declaration list.
    Name name;
    name.temp = name.attribute + 1;
    int a = 12; int i = 0, _b;
    // [test] io statement.
    scanf(_b);
    // [test] while statement and if else ambiguty.
    while (i < 2) {
        if (_b < 0)
            { int c=1;}
        if (_b > 0)
            a = a + _b;
        else {}
        i = i + 1;
    }
    // [test] priority of operations.
    a = a + 2 - 1*_b;
}
